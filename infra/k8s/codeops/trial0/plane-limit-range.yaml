apiVersion: v1
kind: LimitRange
metadata:
  name: codeops-plane-default-resources
spec:
  limits:
    - type: Container
      defaultRequest:
        cpu: 100m
        memory: 256Mi
      default:
        cpu: 500m
        memory: 1Gi
