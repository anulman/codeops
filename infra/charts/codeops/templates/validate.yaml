{{- if or (not .Values.migration.applicationSecretName) (has .Values.migration.applicationSecretName (list .Values.gateway.secretName .Values.migration.secretName .Values.lifecycleRelay.secretName .Values.modelProxy.secretName .Values.postgresql.secretName)) -}}
{{- fail "migration.applicationSecretName must be a separate staged application credential" -}}
{{- end -}}
{{- if or (not .Values.migration.secretName) (eq .Values.migration.secretName .Values.gateway.secretName) (eq .Values.migration.secretName .Values.lifecycleRelay.secretName) (eq .Values.migration.secretName .Values.modelProxy.secretName) -}}
{{- fail "migration.secretName must be separate from every application secret" -}}
{{- end -}}
{{- if not (has .Values.profile (list "full-managed" "full-external" "custom")) -}}
{{- fail "profile must be full-managed, full-external, or custom" -}}
{{- end -}}
{{- if .Values.runtimeEgressProxy.enabled -}}
{{- if not (regexMatch "^[a-z0-9._/-]+$" .Values.runtimeEgressProxy.image.repository) -}}
{{- fail "runtime egress proxy image repository is invalid" -}}
{{- end -}}
{{- if not (regexMatch "^sha256:[0-9a-f]{64}$" .Values.runtimeEgressProxy.image.digest) -}}
{{- fail "runtime egress proxy requires one immutable image digest" -}}
{{- end -}}
{{- if eq (len .Values.runtimeEgressProxy.allowedDomains) 0 -}}
{{- fail "runtime egress proxy requires at least one exact allowed domain" -}}
{{- end -}}
{{- if gt (len .Values.runtimeEgressProxy.allowedDomains) 64 -}}
{{- fail "runtime egress proxy allows at most 64 exact domains" -}}
{{- end -}}
{{- range .Values.runtimeEgressProxy.allowedDomains -}}
{{- if or (ne . (lower .)) (gt (len .) 253) (contains "*" .) (contains "/" .) (contains ":" .) (contains ".." .) (hasPrefix "." .) (hasSuffix "." .) (not (regexMatch "^[a-z0-9][a-z0-9.-]*[a-z0-9]$" .)) -}}
{{- fail "runtime egress proxy allowed domains must be exact lowercase DNS names without wildcards" -}}
{{- end -}}
{{- end -}}
{{- if eq (len .Values.runtimeEgressProxy.allowedConnectPorts) 0 -}}
{{- fail "runtime egress proxy requires at least one CONNECT port" -}}
{{- end -}}
{{- if gt (len .Values.runtimeEgressProxy.allowedConnectPorts) 16 -}}
{{- fail "runtime egress proxy allows at most 16 CONNECT ports" -}}
{{- end -}}
{{- range .Values.runtimeEgressProxy.allowedConnectPorts -}}
{{- if or (lt (int .) 1) (gt (int .) 65535) -}}
{{- fail "runtime egress proxy CONNECT ports must be between 1 and 65535" -}}
{{- end -}}
{{- end -}}
{{- if lt (int .Values.runtimeEgressProxy.replicas) 1 -}}
{{- fail "runtime egress proxy requires at least one replica" -}}
{{- end -}}
{{- end -}}
{{- $fixedSessionOwner := trim .Values.agentsUi.authentication.fixedPrincipal -}}
{{- $sessionOwnerHeader := trim .Values.agentsUi.authentication.principalHeader -}}
{{- if eq (empty $fixedSessionOwner) (empty $sessionOwnerHeader) -}}
{{- fail "agentsUi.authentication requires exactly one fixedPrincipal or principalHeader" -}}
{{- end -}}
{{- if and (not (empty $fixedSessionOwner)) (not (regexMatch "^[A-Za-z0-9][A-Za-z0-9._:@/-]{0,255}$" $fixedSessionOwner)) -}}
{{- fail "agentsUi.authentication.fixedPrincipal is invalid" -}}
{{- end -}}
{{- if and (not (empty $sessionOwnerHeader)) (not (regexMatch "^[a-z0-9][a-z0-9._-]{0,127}$" $sessionOwnerHeader)) -}}
{{- fail "agentsUi.authentication.principalHeader must be one lowercase trusted header name" -}}
{{- end -}}
{{- if not (has .Values.postgresql.deployment (list "managed" "external")) -}}
{{- fail "postgresql.deployment must be managed or external" -}}
{{- end -}}
{{- if not (has .Values.temporal.driver (list "temporal" "none")) -}}
{{- fail "temporal.driver must be temporal or none" -}}
{{- end -}}
{{- if not (has .Values.temporal.deployment (list "managed" "external" "none")) -}}
{{- fail "temporal.deployment must be managed, external, or none" -}}
{{- end -}}
{{- if ne .Values.temporal.enabled (eq .Values.temporal.deployment "managed") -}}
{{- fail "temporal.enabled must be true only for managed deployment" -}}
{{- end -}}
{{- if ne (eq .Values.temporal.driver "none") (eq .Values.temporal.deployment "none") -}}
{{- fail "temporal driver and deployment none must be selected together" -}}
{{- end -}}
{{- if not (has .Values.jetstream.driver (list "jetstream" "none")) -}}
{{- fail "jetstream.driver must be jetstream or none" -}}
{{- end -}}
{{- if not (has .Values.jetstream.deployment (list "managed" "external" "none")) -}}
{{- fail "jetstream.deployment must be managed, external, or none" -}}
{{- end -}}
{{- if ne .Values.jetstream.enabled (eq .Values.jetstream.deployment "managed") -}}
{{- fail "jetstream.enabled must be true only for managed deployment" -}}
{{- end -}}
{{- if ne (eq .Values.jetstream.driver "none") (eq .Values.jetstream.deployment "none") -}}
{{- fail "jetstream driver and deployment none must be selected together" -}}
{{- end -}}
{{- if not (has .Values.plane.deployment (list "managed" "external" "none")) -}}
{{- fail "plane.deployment must be managed, external, or none" -}}
{{- end -}}
{{- if ne .Values.plane.enabled (eq .Values.plane.deployment "managed") -}}
{{- fail "plane.enabled must be true only for managed deployment" -}}
{{- end -}}
{{- if and .Values.plane.adapter.enabled (eq .Values.plane.deployment "none") -}}
{{- fail "Plane adapter cannot be enabled when Plane is disabled" -}}
{{- end -}}
{{- if and .Values.plane.adapter.enabled .Values.plane.adapter.onboardingRequired -}}
{{- fail "Plane adapter cannot be enabled while onboarding is required" -}}
{{- end -}}
{{- $webhookIngress := .Values.githubController.webhookIngress -}}
{{- if $webhookIngress.enabled -}}
{{- if not .Values.plane.adapter.enabled -}}
{{- fail "githubController.webhookIngress requires the Plane adapter" -}}
{{- end -}}
{{- if or (gt (len $webhookIngress.host) 253) (contains ".." $webhookIngress.host) (not (regexMatch "^[a-z0-9]([-a-z0-9.]*[a-z0-9])$" $webhookIngress.host)) -}}
{{- fail "githubController.webhookIngress.host must be one exact lowercase DNS name" -}}
{{- end -}}
{{- if and (not (empty $webhookIngress.className)) (not (regexMatch "^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$" $webhookIngress.className)) -}}
{{- fail "githubController.webhookIngress.className must be one Kubernetes name" -}}
{{- end -}}
{{- if not (regexMatch "^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$" $webhookIngress.tlsSecretName) -}}
{{- fail "githubController.webhookIngress.tlsSecretName must be one Kubernetes Secret name" -}}
{{- end -}}
{{- if or (eq (len $webhookIngress.repositories) 0) (gt (len $webhookIngress.repositories) 64) -}}
{{- fail "githubController.webhookIngress.repositories must contain 1 to 64 repository identities" -}}
{{- end -}}
{{- if ne (len ($webhookIngress.repositories | uniq)) (len $webhookIngress.repositories) -}}
{{- fail "githubController.webhookIngress.repositories must be unique" -}}
{{- end -}}
{{- range $webhookIngress.repositories -}}
{{- if not (regexMatch "^[A-Za-z0-9_.-]{1,100}/[A-Za-z0-9_.-]{1,100}$" .) -}}
{{- fail "githubController.webhookIngress.repositories entries must be owner/repository identities" -}}
{{- end -}}
{{- end -}}
{{- end -}}
{{- if eq .Values.profile "full-managed" -}}
{{- if or (ne .Values.postgresql.deployment "managed") (ne .Values.temporal.deployment "managed") (ne .Values.jetstream.deployment "managed") (ne .Values.plane.deployment "managed") -}}
{{- fail "full-managed requires managed PostgreSQL, Temporal, JetStream, and Plane" -}}
{{- end -}}
{{- end -}}
{{- if eq .Values.profile "full-external" -}}
{{- if or (ne .Values.postgresql.deployment "external") (ne .Values.temporal.deployment "external") (ne .Values.jetstream.deployment "external") (ne .Values.plane.deployment "external") (not .Values.plane.adapter.enabled) -}}
{{- fail "full-external requires external PostgreSQL, Temporal, JetStream, and an enabled Plane adapter" -}}
{{- end -}}
{{- end -}}
{{- if and (eq .Values.postgresql.deployment "external") (not (regexMatch "^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$" .Values.postgresql.external.host)) -}}
{{- fail "external PostgreSQL requires one lowercase DNS host" -}}
{{- end -}}
{{- if and (eq .Values.jetstream.driver "jetstream") (not (regexMatch "^nats://[A-Za-z0-9.-]+:[0-9]{1,5}$" .Values.jetstream.url)) -}}
{{- fail "JetStream requires one exact nats://host:port URL" -}}
{{- end -}}
{{- if and (eq .Values.jetstream.deployment "external") .Values.quickstart.enabled -}}
{{- fail "external JetStream requires existing-Secret mode" -}}
{{- end -}}
{{- if and (eq .Values.postgresql.deployment "external") .Values.quickstart.enabled -}}
{{- fail "external PostgreSQL requires existing-Secret mode" -}}
{{- end -}}
{{- if and .Values.plane.adapter.enabled (not (regexMatch "^https://[A-Za-z0-9.-]+(:[0-9]{1,5})?/?$" .Values.plane.apiOrigin)) -}}
{{- fail "Plane adapter requires one credential-free HTTPS API origin" -}}
{{- end -}}
{{- if .Values.quickstart.enabled -}}
{{- $quickstart := .Values.quickstart -}}
{{- $repository := $quickstart.repository -}}
{{- if not (regexMatch "^[A-Za-z0-9_.-]{1,100}/[A-Za-z0-9_.-]{1,100}$" $repository.identity) -}}
{{- fail "quickstart.repository.identity must be one owner/repository identity" -}}
{{- end -}}
{{- if or (lt (len $quickstart.openaiApiKey) 16) (regexMatch "\\s" $quickstart.openaiApiKey) -}}
{{- fail "quickstart.openaiApiKey must be one non-whitespace credential of at least 16 characters" -}}
{{- end -}}
{{- if $quickstart.registry.enabled -}}
{{- if not (regexMatch "^[a-z0-9.-]+(:[0-9]{1,5})?$" $quickstart.registry.server) -}}
{{- fail "quickstart.registry.server must be one registry host" -}}
{{- end -}}
{{- if not (regexMatch "^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$" $quickstart.registry.secretName) -}}
{{- fail "quickstart.registry.secretName must be one Kubernetes Secret name" -}}
{{- end -}}
{{- if or (eq (trim $quickstart.registry.username) "") (lt (len $quickstart.registry.token) 16) (regexMatch "\\s" $quickstart.registry.token) -}}
{{- fail "quickstart.registry requires one username and one non-whitespace token of at least 16 characters" -}}
{{- end -}}
{{- end -}}
{{- $credentials := list $repository.github.readToken $repository.github.writeToken -}}
{{- if .Values.plane.adapter.enabled -}}
{{- $credentials = concat $credentials (list $repository.github.webhookSecret $repository.plane.apiKey $repository.plane.webhookSecret) -}}
{{- end -}}
{{- range $credentials -}}
{{- if or (lt (len .) 16) (regexMatch "\\s" .) -}}
{{- fail "quickstart repository credentials must be non-whitespace values of at least 16 characters" -}}
{{- end -}}
{{- end -}}
{{- if ne (len ($credentials | uniq)) (len $credentials) -}}
{{- fail "quickstart repository credentials must be authority-scoped and unique" -}}
{{- end -}}
{{- if .Values.plane.adapter.enabled -}}
{{- if eq (len $repository.github.reviewerIds) 0 -}}
{{- fail "quickstart.repository.github.reviewerIds must contain at least one GitHub user ID" -}}
{{- end -}}
{{- $reviewerIds := list -}}
{{- range $repository.github.reviewerIds -}}
{{- $reviewerId := int64 . -}}
{{- if or (le $reviewerId 0) (and (kindIs "string" .) (not (regexMatch "^[1-9][0-9]{0,15}$" .))) -}}
{{- fail "quickstart.repository.github.reviewerIds must contain positive numeric GitHub user IDs" -}}
{{- end -}}
{{- $reviewerIds = append $reviewerIds (toString $reviewerId) -}}
{{- end -}}
{{- if ne (len ($reviewerIds | uniq)) (len $reviewerIds) -}}
{{- fail "quickstart.repository.github.reviewerIds must be unique" -}}
{{- end -}}
{{- if not (regexMatch "^https://[A-Za-z0-9.-]+(:[0-9]{1,5})?/?$" $repository.plane.apiOrigin) -}}
{{- fail "quickstart.repository.plane.apiOrigin must be one credential-free HTTPS origin" -}}
{{- end -}}
{{- if not (regexMatch "^[a-z0-9][a-z0-9-]{0,62}$" $repository.plane.workspaceSlug) -}}
{{- fail "quickstart.repository.plane.workspaceSlug must be one Plane workspace slug" -}}
{{- end -}}
{{- $uuidValues := list $repository.plane.workspaceId $repository.plane.projectId $repository.plane.stateIds.ready $repository.plane.stateIds.inProgress $repository.plane.stateIds.needsAttention $repository.plane.stateIds.complete -}}
{{- $uuidValues = concat $uuidValues $repository.plane.humanActorIds -}}
{{- range $repository.plane.personas -}}
{{- $uuidValues = append $uuidValues .userId -}}
{{- end -}}
{{- range $uuidValues -}}
{{- if not (regexMatch "^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$" .) -}}
{{- fail "quickstart Plane identities and lifecycle states must be lowercase UUIDs" -}}
{{- end -}}
{{- end -}}
{{- $stateIds := list $repository.plane.stateIds.ready $repository.plane.stateIds.inProgress $repository.plane.stateIds.needsAttention $repository.plane.stateIds.complete -}}
{{- if ne (len ($stateIds | uniq)) 4 -}}
{{- fail "quickstart repository Plane lifecycle state IDs must be unique" -}}
{{- end -}}
{{- if eq (len $repository.plane.humanActorIds) 0 -}}
{{- fail "quickstart.repository.plane.humanActorIds must contain at least one Plane user ID" -}}
{{- end -}}
{{- if ne (len ($repository.plane.humanActorIds | uniq)) (len $repository.plane.humanActorIds) -}}
{{- fail "quickstart.repository.plane.humanActorIds must be unique" -}}
{{- end -}}
{{- if ne (len $repository.plane.personas) 7 -}}
{{- fail "quickstart.repository.plane.personas must contain all seven CodeOps personas" -}}
{{- end -}}
{{- $personaHandles := list -}}
{{- range $repository.plane.personas -}}
{{- $personaHandles = append $personaHandles .handle -}}
{{- end -}}
{{- range (list "@ai-web" "@ai-security" "@ai-database" "@ai-infra" "@ai-design" "@ai-product" "@ai-ml") -}}
{{- if not (has . $personaHandles) -}}
{{- fail "quickstart.repository.plane.personas must contain every supported persona handle exactly once" -}}
{{- end -}}
{{- end -}}
{{- if ne (len ($personaHandles | uniq)) 7 -}}
{{- fail "quickstart.repository.plane.personas handles must be unique" -}}
{{- end -}}
{{- $personaUserIds := list -}}
{{- range $repository.plane.personas -}}
{{- $personaUserIds = append $personaUserIds .userId -}}
{{- end -}}
{{- if ne (len ($personaUserIds | uniq)) 7 -}}
{{- fail "quickstart.repository.plane.personas user IDs must be unique" -}}
{{- end -}}
{{- end -}}
{{- if not (regexMatch "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" $repository.context.directory) -}}
{{- fail "quickstart.repository.context.directory must be one lowercase path component" -}}
{{- end -}}
{{- range (list $repository.context.currentState $repository.context.decisions $repository.context.domain $repository.context.product $repository.context.sourceMap) -}}
{{- if or (eq (trim .) "") (gt (len .) 100000) -}}
{{- fail "quickstart repository-specific context documents must contain 1 to 100000 bytes" -}}
{{- end -}}
{{- end -}}
{{- range (list $repository.context.agents $repository.context.soul) -}}
{{- if gt (len .) 100000 -}}
{{- fail "quickstart baseline context overrides must contain at most 100000 bytes" -}}
{{- end -}}
{{- end -}}
{{- end -}}
{{- if not (regexMatch "^[0-9a-f]{40}$" .Values.githubController.controlPlaneSha) -}}
{{- fail "githubController.controlPlaneSha must be one exact lowercase Git SHA" -}}
{{- end -}}
{{- if not (regexMatch "^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$" .Values.githubController.repositoryAuthoritySecretName) -}}
{{- fail "githubController.repositoryAuthoritySecretName must be one Kubernetes Secret name" -}}
{{- end -}}
{{- if not (regexMatch "^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$" .Values.gateway.repositorySteeringRegistrySecretName) -}}
{{- fail "gateway.repositorySteeringRegistrySecretName must be one Kubernetes Secret name" -}}
{{- end -}}
{{- if not (regexMatch "^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$" .Values.controlGateway.secretName) -}}
{{- fail "controlGateway.secretName must be one Kubernetes Secret name" -}}
{{- end -}}
{{- if not (regexMatch "^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$" .Values.controlGateway.repositoryAuthoritySecretName) -}}
{{- fail "controlGateway.repositoryAuthoritySecretName must be one Kubernetes Secret name" -}}
{{- end -}}
{{- if .Values.proofPublisher.enabled -}}
{{- if lt (int .Values.proofPublisher.replicas) 1 -}}
{{- fail "proofPublisher requires at least one replica" -}}
{{- end -}}
{{- if not (regexMatch "^[A-Za-z0-9][A-Za-z0-9:._/-]{0,199}$" .Values.proofPublisher.destinationId) -}}
{{- fail "proofPublisher.destinationId is invalid" -}}
{{- end -}}
{{- range (list .Values.proofPublisher.s3.endpoint .Values.proofPublisher.s3.publicBaseUrl) -}}
{{- if not (regexMatch "^https://[A-Za-z0-9.-]+(:[0-9]{1,5})?/?$" .) -}}
{{- fail "proofPublisher S3 URLs must be credential-free HTTPS origins" -}}
{{- end -}}
{{- end -}}
{{- if not (regexMatch "^[a-z0-9][a-z0-9.-]{1,62}[a-z0-9]$" .Values.proofPublisher.s3.bucket) -}}
{{- fail "proofPublisher.s3.bucket is invalid" -}}
{{- end -}}
{{- if not (regexMatch "^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$" .Values.proofPublisher.s3.region) -}}
{{- fail "proofPublisher.s3.region is invalid" -}}
{{- end -}}
{{- range (list .Values.proofPublisher.s3.credentialSecretName .Values.proofPublisher.auth.secretName) -}}
{{- if not (regexMatch "^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$" .) -}}
{{- fail "proofPublisher Secret names are invalid" -}}
{{- end -}}
{{- end -}}
{{- if eq .Values.proofPublisher.s3.credentialSecretName .Values.proofPublisher.auth.secretName -}}
{{- fail "proofPublisher S3 credential and auth token must use distinct Secrets" -}}
{{- end -}}
{{- range (list .Values.proofPublisher.s3.accessKeyIdKey .Values.proofPublisher.s3.secretAccessKeyKey .Values.proofPublisher.auth.tokenKey) -}}
{{- if not (regexMatch "^[A-Za-z0-9._-]+$" .) -}}
{{- fail "proofPublisher Secret data keys are invalid" -}}
{{- end -}}
{{- end -}}
{{- if or (lt (int .Values.proofPublisher.retentionDays) 1) (gt (int .Values.proofPublisher.retentionDays) 3650) -}}
{{- fail "proofPublisher.retentionDays must be from 1 through 3650" -}}
{{- end -}}
{{- end -}}
{{- if .Values.controlGateway.webPush.enabled -}}
{{- if not (regexMatch "^[A-Za-z0-9_-]{80,128}$" .Values.controlGateway.webPush.publicKey) -}}
{{- fail "controlGateway.webPush.publicKey must be one VAPID public key" -}}
{{- end -}}
{{- if not (regexMatch "^(https://|mailto:)[^[:space:]#]+$" .Values.controlGateway.webPush.subject) -}}
{{- fail "controlGateway.webPush.subject must be one HTTPS or mailto VAPID subject" -}}
{{- end -}}
{{- if not (regexMatch "^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$" .Values.controlGateway.webPush.secretName) -}}
{{- fail "controlGateway.webPush.secretName must be one Kubernetes Secret name" -}}
{{- end -}}
{{- if not (regexMatch "^[A-Za-z0-9._-]+$" .Values.controlGateway.webPush.privateKeyKey) -}}
{{- fail "controlGateway.webPush.privateKeyKey must be one Secret data key" -}}
{{- end -}}
{{- end -}}
{{- if not (regexMatch "^sha256:[0-9a-f]{64}$" .Values.runtime.releaseDigest) -}}
{{- fail "runtime.releaseDigest must be one lowercase SHA-256 digest" -}}
{{- end -}}
{{- range (list .Values.runtime.profileId .Values.runtime.compatibilityPolicyRevision) -}}
{{- if not (regexMatch "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$" .) -}}
{{- fail "runtime profile and compatibility-policy identities are invalid" -}}
{{- end -}}
{{- end -}}
{{- if eq (len .Values.controlGateway.kubernetesApiCidrs) 0 -}}
{{- fail "controlGateway.kubernetesApiCidrs must contain the exact Kubernetes API endpoint CIDR" -}}
{{- end -}}
{{- range .Values.controlGateway.kubernetesApiCidrs -}}
{{- if not (regexMatch "^[0-9]{1,3}(\\.[0-9]{1,3}){3}/[0-9]{1,2}$" .) -}}
{{- fail "controlGateway.kubernetesApiCidrs entries must be IPv4 CIDRs" -}}
{{- end -}}
{{- end -}}
{{- if and (eq .Values.temporal.driver "temporal") (not (regexMatch "^[A-Za-z0-9.-]+:[0-9]{1,5}$" .Values.temporal.address)) -}}
{{- fail "temporal.address must be an exact host:port" -}}
{{- end -}}
{{- $contextDirectories := dict -}}
{{- range .Values.githubController.repositoryContexts -}}
{{- if not (regexMatch "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$" .directory) -}}
{{- fail "githubController.repositoryContexts directory must be one lowercase path component" -}}
{{- end -}}
{{- if hasKey $contextDirectories .directory -}}
{{- fail "githubController.repositoryContexts directories must be unique" -}}
{{- end -}}
{{- $_ := set $contextDirectories .directory true -}}
{{- if not (regexMatch "^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$" .secretName) -}}
{{- fail "githubController.repositoryContexts secretName must be one Kubernetes Secret name" -}}
{{- end -}}
{{- end -}}
