# Agent instructions

Start with the repository root `AGENTS.md`. Treat repository identity and
credential selection as security boundaries. Run focused tests during a
change. Do not incorporate copyleft or source-available code or package
dependencies. Prefer a permissively licensed alternative and stop on an
unknown license. Run `nub run verify` before handoff. Do not publish, merge,
or deploy without explicit authority.
